Basic setup:
1. Install node.
2. Modify config.js, choosing your 'webRoot' and 'port'.
3. Run httpd.sh on *nix, httpd.bat on W*ndows.
4. Point your browser to http://localhost:8081 (if 'port' is 8081),
   and see a basic web server that serves up static files
   and directory indexes.
